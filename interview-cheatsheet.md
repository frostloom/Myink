# Ai Ink 面试备战手册（Agent / AI 项目高频问题应答）

> 基于 2026 年真实面经（字节/阿里/百度/腾讯）的高频问题整理，答案全部绑定本项目的真实实现与数字。
> 面试前过一遍：**每个答案结尾带一个数字或一个 trade-off**，被问住就说「这是我 MVP 没做的，如果上生产我会……」。
>
> **章节顺序 = 面试真实提问顺序**：开场介绍 → 答题心法 → 系统难点总览（面试第一问）→ Agent 概念 → 架构 → LangGraph 机制 → 记忆 → 一致性 → 结构化输出 → 工程化 → 接口 → 数据库 → 评测 → 踩坑。难点章是「综合答案」，细节章是「被追问就深挖」的弹药库。

---

## 一、开场项目介绍（必考，30 秒定生死）

**Q：介绍一下你做的这个项目。**

**STAR + 因果链答法（先业务后技术，不要一上来倒技术栈）：**

> 这是一个面向**长篇网文连续创作**的多 Agent 系统。长篇几十万字，AI 单遍生成会出四大问题：**人设不一致、战力崩坏、剧情前后矛盾、长上下文过载**。我的系统用「**规划 → 生成 → 校验 → 记忆沉淀**」的闭环解决这些问题。
>
> - **谁把自然语言变成可执行步骤？** Planner——一次规划 N 章的推进蓝图。
> - **谁写正文？** Writer。
> - **谁抽记忆？** Extract——把正文结构化抽成人物/事件/关系候选。
> - **谁校验？** 确定性规则层 L1 + LLM 语义层 Audit。
> - **失败后状态落在哪？** 三层：LangGraph checkpoint 断点、记忆候选池（待人工确认）、任务表。
>
> 结果量化：40 条冲突样例评测**检出率 80.8%、误报率 0%**，466 项 Python 测试，35 条路径 OpenAPI 契约回归闸防漂移。

**为什么这样开场**：面试官要「能画出来的因果链」——业务解决什么问题、输入输出边界在哪、谁把自然语言变成可执行步骤、谁调工具、谁汇总、失败后状态落在哪一层。讲完这些再提 LangGraph 只是「我用图来承载这条链」。

---

## 二、通用答题心法（所有题适用，先背这个）

1. **每个答案结尾带一个数字或一个 trade-off**：「我用了 X 而不是 Y，因为 Z」——区分「调包侠」和工程能力的分水岭。
2. **被问住就说「这是我 MVP 没做的，如果上生产我会……」**——诚实 + 有后续方案，比硬编可信。
3. **别一上来倒技术栈**，先讲业务因果链，技术是承载因果链的手段。
4. **多 Agent 编排、状态机流转、智能体协作、异常容错**是必考，必须依托项目完整拆解，别只背八股。

---

## 三、系统难点与解决方案（面试第一问的必答题，最大亮点）

> 面试官几乎必问「项目里最大的难点是什么」。答案要**一句话说清难点，展开讲清方案，结尾落一个数字或 trade-off**。下面四个按「最硬」排序，任选 2 个讲透即可，别全倒。**每个难点都在本手册有详细展开章**——先给综合答案，被追问就往对应章深挖。

**难点 1：长文一致性——AI 写 100 万字，主角战力/关系/伏笔怎么不漂移？**（详见「幻觉治理 / 一致性校验」章）

- **一句话**：LLM 单遍生成记不住设定，且不能靠「每章无限重写」保一致——成本扛不住。
- **三层防线**：
  1. **写前预防**：recall 注入台账快照 + 硬约束恒在 Top-K + 整书大纲位——让模型「带着设定写」而不是「凭空写」。
  2. **写后校验**：L1 确定性规则（9 类，**零 LLM 成本**）+ L2 LLM-as-judge（带 `evidence` 逐字引用 + `confidence`，不是凭感觉）。
  3. **数据边界**：extract 只产候选进确认池，**用户确认才落库**——幻觉记忆永远进不了事实层。
- **数字**：40 条冲突样例（阳性 26 + 阴性 14），检出率 80.8%、误报率 0%；L1 校验零成本。

**难点 2：成本控制——每章至少 4 次 LLM 调用，长篇批量怎么不烧钱？**（详见「工程化」章）

- **一句话**：20 章批次成本线性膨胀，要省每一章、又不牺牲质量。
- **五板斧**：
  1. **前缀缓存**：DeepSeek 磁盘前缀缓存自动命中同前缀 system 指令，**价差约 50 倍**——所以各 Agent 的 system prompt 刻意保持稳定不随章变化。
  2. **JSON mode + thinking disabled**：结构化输出（extract/plan/audit）和长文正文都关思考模式，省推理 token。
  3. **预算闸门**：max_revisions=2、max_replans=1（重规划比重写贵）、max_tool_calls=3、批次硬上限 20。
  4. **L1 把一半校验从 LLM 挪到规则**：能确定性判的绝不上 LLM。
  5. **批次级共享池**：hard_facts 组装一次复用，省重复组装 token。
- **数字**：50 倍价差、L1 零成本、12k 召回预算。

**难点 3：多 Agent 协作 + 断点续跑——单章失败不能整批作废，人工确认后要能续跑**（详见「LangGraph 核心机制」+「工程化」章）

- **一句话**：batch 主图 + chapter 子图两级编排，失败要能断点续跑而不是整批重来。
- **三点**：
  1. **异常式中断**：失败抛 `BatchChapterError` 而非返回 state——LangGraph 只在图未到 END 时能 resume，抛异常让 checkpoint 停在章节节点。
  2. **确认流收尾**：`finalize_awaiting_review` 直接取 checkpoint 草稿（含 write 产物）落库，不走图重跑。
  3. **幂等守卫**：池判重（同 payload 不重复进池）、新人物卡片去重（名字已建档不抽）——防 resume 重跑堆重复、死循环。
- **故事**：LangGraph 1.2.9 的 `invoke-resume` 语义是**整图从 START 重跑不是断点续跑**——用户实测「确认后点续跑变成重新生成」→ 改收尾路径不重跑图。「我知道框架语义和直觉不一样。」

**难点 4：多租户数据隔离——上千本书的向量/记忆/checkpoint 不能互相污染**（详见「工程化」+「数据库选型」章）

- **一句话**：连接池复用可能串租户；向量检索不显式过滤会拿他书向量污染 Top-K。
- **四点**：
  1. RLS FORCE + 事务级 `SET LOCAL app.tenant_id`（事务结束自动恢复，防连接池污染）。
  2. 业务角色 `NOBYPASSRLS` + admin/业务双角色分离（超级用户永远绕过 RLS，只能做 DDL/迁移）。
  3. 向量查询显式 `project_id` filter 走 HNSW **过滤索引**——先过滤后排序。
  4. checkpoint 表无 RLS → 靠 thread_id 归属校验兜底（**知道这个边界本身就是加分**）。
- **坑**：`SET LOCAL` 提交后会话级 tenant 是 `''` 不是 NULL，策略用 `NULLIF(current_setting,'')` 判空，否则裸 `::uuid` 会 DataError。

---

## 四、Agent 基础概念 + 本项目定位（高频陷阱题）

**Q：Agent 核心组件？和 Workflow / 普通链式调用的本质区别？**

**口径**：核心组件是**规划、记忆、工具调用**三件套，核心特征是「行动-观察」循环闭环决策。但诚实说——**本项目本质是确定性 Workflow + 局部 Agent 决策点，这是刻意设计**：

- 流程控制权在**代码**（两层图结构）手里 → Workflow 特征
- 只有被授权的**语义层**由 LLM 自主决策 → audit 节点输出 pass/rewrite/replan 三级路由
- 为什么刻意这样？创作要**可复现、可审计、不烧钱**。LLM 全自主的话，一个死循环能烧掉几十次调用，正文还可能漂移。

> 加分点：证明你真懂「控制权在代码 vs 在 LLM」的区别——这正是字节一面原题。

**Q：ChatBot + 插件算 Agent 吗？RAG + Chat 算吗？**

**口径**：插件按关键词固定路由 = 带工具的 Bot，不是 Agent；模型多步推理中**自主选择工具形成闭环**才算。本项目里 write/audit 节点持**只读查证工具**（`READ_TOOLS`），模型在循环里自主决定查不查——这才是本系统 agentic 的点。

---

## 五、架构取舍：为什么多 Agent / 为什么 LangGraph（架构三连问）

**Q：为什么拆成这么多 Agent？**

**口径**：Planner / Writer / Memory / Audit / Revise 各司其职。
1. **单 Agent 长 Prompt 注意力分散**——「规划+写作+抽取+校验」塞一个 Prompt 会互相干扰；
2. **可单测**——每个节点独立验证（test_flow 逐节点断言），职责分离才好维护。

**Q：为什么选 Hierarchical（两层图）而不是 Router / Pipeline？**

**口径**：创作流程有**天然先后依赖**（先规划才能写、写完才能校验），不是无序并行任务，所以 Router 不适用；但单章也不是简单线性 Pipeline——因为要**批量规划 N 章 + 批次失败断点续跑 + 批次收尾复盘**，这必须外层再套一个批次主图。

**Q：LangGraph 比 LangChain 好在哪？有什么缺点？（经典三连）**

**口径**：LangChain 的 AgentExecutor 把 Think-Act-Observe 循环**封装死了，出问题只能靠猜**；LangGraph 用 `add_conditional_edges` 把分支和循环**显式表达**，可控、可持久化、可人审。

**缺点要主动讲**：如果图退化成「调模型→调工具→调模型」的线性三步，框架复杂度不划算。本项目两层图**没有过度设计**——续跑、不重跑已完成章、批量复盘这些需求，线性脚本根本实现不了。

**Q：你的编排和简单 if-else 路由有什么区别？**

**口径**：if-else 只能「选 A 还是选 B」。我这里可以**「先规划 N 章、每章走完整子图、各章结果桥接、批次结束复盘提炼经验注入后续章节」**——有状态共享（批次级 `shared_context`）、有条件分支（`route_after_chapter` 五种走向）、有循环（revise/replan）、有断点续跑、有人工介入（候选池）。核心差异：**图把「状态 + 分支 + 持久化」从业务代码里抽出去了**。

---

## 六、LangGraph 核心机制深挖（背八股，用自己的项目佐证）

**Q：State 怎么设计？TypedDict 还是 Pydantic？**

**口径**：用 TypedDict（`src/aiink/workflow/state.py`），节点之间传**结构化对象**（Pydantic model_dump 的 dict），不传不断变长的自然语言 Prompt。`ChapterState` 有 20+ 字段，`BatchState` 有批次专属字段，两个图各用各的状态。为什么要结构化：**Prompt 会不断变长污染状态，结构化对象类型安全、可调试、断点续跑时快照体积小**。

**Q：断点续跑怎么做？Checkpointer 怎么选？（最容易被深挖，重点背）**

**口径**：
- 用 **PostgreSQL PostgresSaver** 不用 SQLite/Redis——checkpoint 是任务现场，丢了用户从头重跑，必须落有备份/PITR 的 PG；Redis 只管可重建数据。
- **thread_id = task_id**：批次主线程用 `batch_task_id`，单章子线程用 `batch_task_id:ch{seq}` 派生，整批可续跑。
- **关键设计**：单章失败**抛异常而非返回状态**（`src/aiink/workflow/batch_graph.py` `BatchChapterError`）。因为 LangGraph 只有图**未达 END** 才能同 thread 断点续跑；若优雅走到 batch_end 图已 END，续跑会**整批重跑**。抛异常让 checkpoint 停在 chapter 节点（position 不推进），续跑只重跑失败章、**已完成章不重跑**。
- critical 冲突转人工也是同一机制：`BatchReviewError` 使 checkpoint 停在本章，人工确认后 resume 从本章续跑。

**Q：Human-in-the-Loop 怎么做？**

**口径**（差异化答法）：没用 LangGraph 原生 `interrupt`，而是用**业务层状态机**承载人审：记忆候选进 `MemoryCandidate` 池 + 章节状态标 `awaiting_review` + 批次任务状态停 `paused`，人工确认候选后 resume 放行。为什么不用框架 interrupt？**DB 状态人审可审计、可查询、可断线恢复，且不被框架升级绑架**。

**Q：并行呢？**

**口径**（诚实 + 有理由）：生成**同书串行、异书并行**，由 Redis 书锁保证（`defer` 退避重投）。为什么串行？创作有严格时序依赖（每章依赖上章 persist 的结果），强行并行会破坏状态桥。这是刻意取舍。

---

## 七、记忆系统 + 混合召回（字节一面原题级高频 + RAG 岗必问）

**Q：短期/长期记忆怎么设计？**

**口径**（三层，`src/aiink/memory/recall.py`）：
- **短期**：上一章摘要（80-150 字）
- **中期**：事件（结构化，每事件是天然最小检索单元）
- **长期**：事实 hard_facts，**硬约束恒在 Top-K，不参与相似度截断**——设定永远不丢
- **写入规范**：区分事实/推断、带 `source_chapter` 来源可追溯、失效重建（rewrite 先失效旧记忆再写新，**同事务原子**：新写失败回滚则旧记忆不失效）

**Q：上下文越来越大怎么办？**

**口径**：**12k token 预算**分层截断（`recall_token_budget=12000`）；中文 token 估算 1 字≈1.4 token 不引 tokenizer；**批次级共享池**——稳定部分（hard_facts）批次内组装一次、各章复用，省重复组装 + 章间版本一致；变化部分（近期事件/伏笔/快照）每章照查。

**Q：有遗忘机制吗？**

**口径**（诚实 + 有理由）：MVP 没做时间衰减，用的是**结构化台账 + 预算截断**。不衰减是有意为之——创作场景里「设定」不能遗忘（主角死没死、战力上限），所以硬约束恒在 Top-K；真正会膨胀的中期事件靠 `_CONTENT_CAP` 截断和近期优先控制。**遗忘策略看业务，小说设定是权威数据，不适合 LRU 式驱逐**。

**Q：召回策略具体怎么跑？（追问高频）**

**口径**（一次组装、按节点分通道）：
- `node_recall` 调 `build_context` **组装一次**完整 `RetrievedContext`（硬约束/事件/短上下文/人物快照/伏笔/剧情线/写作经验），每个 LLM 节点**只挑自己职责需要的通道**注入 prompt：规划拿伏笔+剧情线（决定收/延/弃）+前情事件；写作拿硬约束+快照+文风+大纲位；抽取只拿台账快照+伏笔（校准 old_value + foreshadow_id 溯源）；audit 拿事件+伏笔+剧情线。
- **硬约束恒在 Top-K**：`get_hard_facts` 把 is_hard 事实无条件排在前面、不参与相似度截断——设定是权威数据永不丢。
- **混合召回**：向量腿（bge-m3 编码上一章摘要 → event 向量近邻 top-5）+ 关键词腿（人物名对事件摘要 ILIKE top-5）→ **RRF 融合**（k=60，双命中跨腿加分）。
- **预算**：12k token，中文 1 字≈1.4 token 估算，`_CONTENT_CAP=300` 截断单条。
- **批次级共享池**：hard_facts 批次内组装一次、各章复用（省重复组装 + 章间版本一致）。
- **向量库被谁用到**：**直接调用**只有 3 处——recall 混合召回、L1 桥段重复校验、全局审计桥段切片；**消费结果**上规划/audit 直接用、写作经章节计划二次传递、抽取不用（语义近似不服务精确对照）。「谁调库」和「谁用结果」要分开答。

**Q：为什么混合检索？BM25 + 向量为什么好？**

**口径**：纯向量检索三个致命问题——精确匹配不行、专业术语召回差、专有名词（人名/地名）遗漏。本项目正好对应：**向量腿**（bge-m3 对上一章摘要语义近邻）+ **关键词腿**（人物名对事件摘要 ILIKE 精确匹配），**RRF 融合**（`k=60`，双命中跨腿加分）。

**差异化点**：关键词腿用 **ILIKE 而非 BM25**。为什么？**中文无分词**，人物名精确子串匹配比 BM25 词频权重在这个场景简单可靠；且写明「ILIKE 走不了 btree 索引，用显式 project_id 过滤 + LIMIT 封顶」——知道这个 trade-off 就是加分项。

**Q：Chunk 切割策略？**

**口径**（差异化）：**不切 chunk**。通用 RAG 要切文档因为知识是无结构长文；但写作系统的记忆是**以「事件/事实」为单位的原子化结构化存储**——每个事件（摘要+置信度+来源章）就是天然的最小检索单元，不需要 chunk 和 overlap。「我的场景不需要 chunk 因为数据已原子化」比背 chunk_size 参数高级。

**Q：Rerank 做了吗？**

**口径**：没做独立 rerank 层，用的是 **RRF 跨腿融合 + 双腿去重排序**（每条腿内已按质量排序）。诚实说明取舍：事件召回量级小（top-K ≤5），再上 Cross-Encoder 精排收益不大还加延迟——**MVP 阶段 RRF 融合够了，量级上来再上 rerank**。

---

## 八、幻觉治理 / 一致性校验（最高频，项目最硬）

**Q：怎么减少幻觉 / 保证内容一致性？**

**口径**（写前预防 + 写后校验 + 数据边界三层）：
1. **写前预防**：recall 注入台账快照 + 硬约束 + 整书大纲位（每章 goal 贴合对应大纲位、沿卷 KR 推进）——让模型「带着设定写」而不是「凭空写」。
2. **写后校验**：L1 确定性规则（零成本）+ **L2 LLM-as-judge**——引用证据 `evidence` + 置信度 `confidence`，不是凭感觉给结论。
3. **数据边界**：extract 只产出候选进池，**用户确认后落库**；critical 冲突自动暂停批次转人工——**幻觉记忆永远进不了事实层**。

**Q：多个 Agent 任务冲突怎么处理？（本项目皇冠）**

**口径**（规则层优先，LLM 不能绕过）：
- **L1 确定性规则**零模型成本拦截硬约束：境界跳级/越界、死而复生、阵营敌我协作（hostile 双名共现 + 协作标记）、战力通胀（连续 3 章每章升境界）、关系台账自洽（重复/矛盾活跃行）、伏笔烂尾（planted 15 章/developing 10 章）、主线停滞（15 章）。
- **预算内修正**：L1 critical / L2 major → `revise` 重写，预算 `max_revisions=2`、`max_replans=1`。
- **超预算转人工**：`needs_review`，候选进池、章节 `awaiting_review`、批次暂停，人工确认后 checkpoint 续跑。

**Q：怎么控制校验/审查 Agent 的误判率？**

**口径**：两阶段——**规则引擎（高召回、零成本）+ LLM 语义层（高精确）**。规则先跑，命中硬约束直接走 revise/转人工，不走 LLM 省成本；LLM 只在规则放行时做语义兜底。直接给检出率/误报率数字。

**加分点——误报控制**：呼应豁免词表（「当年」「重演」）保证刻意呼应不误报；临时盟约守卫（relation 带 `valid_to`）保证合法联手不误报。**这就是 0 误报的工程来源**——不是运气，是刻意「宁缺毋滥，先保阴性 0 误报，再抬阳性检出率」。

**Q：检索-生成对齐怎么做的？（防模型生成与台账脱节）**

**口径**（近免费实现）：`outline_deviation`——规划时 `expected_events` vs extract 抽出的实际事件做比对，缺项报偏差，让作者决策「改正文还是改大纲」。**关键是复用 extract 产物，不新增 LLM 调用**——工程手段解决对齐，不烧钱。

另一个：**桥段重复向量近邻**——当前章事件候选 vs 历史事件余弦近邻，距离 < 0.3 且章距 ≥ 10 判定偷懒重复桥段。这是「生成后自校验」的落地。

---

## 九、LLM 输出不是严格结构化数据怎么办（最高频实操题）

> 面试官问「LLM 返回的不是严格 JSON 怎么解决」，考的是「你有没有被 LLM 不确定性坑过 + 怎么工程化兜住」。**一句话先定调，然后按防线展开，最后落一个 inkos 演化故事**。

**核心一句话**：LLM 结构化输出不能靠承诺，要靠防御——**请求侧强制、解析侧容错、校验侧拒绝、失败侧自愈**，四道防线各兜一类问题。

**防线 1：请求侧强制（让它尽量别坏）**
- **JSON mode**：`response_format={"type":"json_object"}`，API 层约束输出是合法 JSON。
- **关思考**（`thinking: disabled`）：v4 思考默认开，思考 token 会让 content 变空、正文进 `reasoning_content`、还冲散 JSON 结构 → 结构化节点（plan/extract/audit）全关。
- **提示词写死格式**：`SYSTEM_AUDIT` 要求严格 JSON verdict、`SYSTEM_WRITE` 要求 `=== CONTENT ===` 标记。

**防线 2：解析侧容错（`_parse_json` 三级递进，坏得离谱也能救回）**
1. `raw_decode` 只取**第一个完整 JSON 值** → 天然截断前后杂质（markdown 围栏 / 语气词 / 尾部废话）。
2. 找不到就从第一个 `{` 起，**状态机只转义字符串值内的原始控制字符**（结构空白保留）。
3. 再不行就**修复裸 ASCII 引号**（正文里的 `"` 提前闭合字符串）。
- **演进细节（加分）**：早期用无差别正则删控制字符，把结构位置的合法换行也转义了 → 多行 JSON 必挂（2026-08-10 演示逼出）→ 改成状态机只在字符串内转义。

**防线 3：校验侧拒绝（结构对但内容不合法）**
- **Pydantic 校验**：`ValidationError` → 丢弃该候选不中断整章（坏候选拒绝但不崩）。
- **白名单**：`CHARACTER_STATE_FIELDS` / `RELATION_TYPES` —— LLM 自由输出造出的枚举外值（如 `realm_status`）落库必炸批次，进 persist 前丢弃。
- **归一化失败即丢**：人名对不上登记表 → 丢弃。

**防线 4：失败侧自愈（`_llm_checked` 修正重试）**
- check 解析失败 → **重发同请求 + 一行修正指令**（`_RETRY_CORRECTIVE_JSON` / `_RETRY_CORRECTIVE_TEXT`），最多 `_LLM_CHECK_RETRIES=2` 次，靠模型方差 + 明确纠正挽回。
- **关键语义**：只重试解析层失败；`resp.error`（模型降级链已用尽）**直接透传不重试**——降级链都失败说明模型本身不行，重试同一模型无意义。
- 每轮 `record_run` 记一条，前端流转图可见「重试」。

**兜底：模型降级链 + 诚实失败**
- FallbackChain（flash→pro）；仍失败 → BatchFailStub 建模「**失败一次→批次失败**」，不静默重试把失败吃掉。
- content 空但 `reasoning_content` 有内容 → 用它兜底；都空 → 快速重试（不 sleep）。

**这块参考了 inkos（演化故事，面试加分）**
- 骨架三处对应：标记锚点 `=== TAG ===` 切块（write 的 `=== CONTENT ===`）、JSON 净化 + schema 校验（sanitizeJSON/stripCodeFence/Zod）、校验失败带反馈重试（`retrySettlementAfterValidationFailure`）。
- **但每处都演化了**：inkos 无差别正则 → ai-ink 状态机；pipeline 级重跑 → 节点级 `_llm_checked` 且只重试解析层失败。
- **话术**：「我参考了 inkos 的骨架，但每档都做了演化——解析器从无差别正则改成字符串内转义的状态机，因为无差别正则会在多行 JSON 上翻车；重试从 pipeline 级收敛到节点级，且只重试解析层失败、降级链已用尽的失败直接透传，不浪费重试预算。」

---

## 十、工程化：重试 / 幂等 / 成本 / 多租户（考验「能不能扛事」，有真货）

**Q：某个 Agent 挂了 / 超时 / 返回坏了怎么办？（三层防御，全在代码里真实存在）**

**口径**：
1. **调用层**（`src/aiink/providers/deepseek.py`）：指数退避 1s/2s/4s 最多 3 次；空白 content 快速重试（不 sleep）；SDK 自带重试关掉（`max_retries=0`）——**重试由本层控制，可观测、记日志**。
2. **任务层**（`src/aiink/worker/processor.py`）：worker 重试 3 次、`2^retry` 秒退避；**429/5xx/网络可重试，4xx 不可重试**——可重试失败进延迟队列，不可重试进 DLQ；书锁忙是瞬态不算失败，defer 退避重投不计 retry_count。
3. **图层**：checkpoint 断点续跑，失败章从断点继续、不重跑已完成章。
- **超时**：provider 120s；网关对长生成端点用 180s 长转发（坑：30s 转发超时对长生成会 502）。
- **降级**：模型降级链（FallbackChain）——两档模型同病白给时兜底；embedding 挂了降级纯关系召回（`EMBED_ENABLED=0` 时不加载 bge-m3，encode 抛错由调用方降级）；向量腿/关键词腿**独立降级**；MCP 扫榜网络不可达返回内置样例——**降级都标 degraded 可观测**。

**Q：幂等怎么保证？（重试风暴相关）**

**口径**（多处真实落地）：
- worker 消费前查任务状态：已处理/取消/他 worker 在跑 → `skip` 直接 ACK 丢弃
- persist 幂等守卫：`_pool_has_duplicate` 防同 payload 候选重复进池（resume 重跑 extract 不堆重复）
- 新人物卡片去重：名字已建档 → 不抽为候选（防死循环：resume 重跑把已确认角色反复抽成新卡 → 永久 awaiting_review 正文永不落库）
- 批次成本按 `task_id LIKE {batch_id}%` 前缀聚合，防漏章

**Q：成本控制？（Agent 岗位必问）**

**口径**（每个都是真实数字/实现）：
- **前缀缓存命中**：DeepSeek 磁盘前缀缓存自动命中同前缀 system 指令，**价格差约 50 倍**——所以各 Agent 的 system prompt 刻意保持稳定不随章变化
- **JSON mode + thinking disabled**：结构化节点（extract/plan/audit）和长文正文都关思考模式，省推理 token——单遍生成不需要思考
- **预算闸门**：`max_revisions=2`、`max_replans=1`（重规划比重写贵，预算更紧）、`max_tool_calls=3`、批次硬上限 20
- **L1 把一半校验从 LLM 挪到规则**：能确定性判的绝不上 LLM
- **批次级共享池**：hard_facts 组装一次复用，省重复组装 token
- **批次成本展示**：前端闭环展示每任务总花费——成本透明

**Q：多租户 / 数据隔离？**

**口径**：PostgreSQL **RLS + 事务级 `SET LOCAL`** 杜绝连接池跨租户串数据（坑：`SET LOCAL` 提交后会话级 tenant 是 `''` 不是 NULL，策略用 `NULLIF(current_setting,'')` 判空）；业务角色 `NOBYPASSRLS`（防超户绕过）+ admin/业务双角色分离；向量检索显式 `filter` 走 HNSW 过滤索引保召回质量；网关 JWT 验签 + 应用层归属断言双保险。**专业细节**：LangGraph checkpoint 三表不带 project_id、没 RLS，靠 thread_id 归属校验——知道这个边界直接加分。

---

## 十一、接口设计（后端/网关岗必问）

**Q：接口怎么设计的？为什么这样？**

**口径**：
- **架构**：唯一入口 = Go/Gin 网关 :8080（JWT 鉴权 + 令牌桶限流 + 三层闸门 + SSE + 转发），转发 Python API :8100；前端静态产物同源托管，单端口出页面 + API。
- **路由前缀**：`/api/v1`；`POST /auth/token` 签发 JWT（不挂中间件，否则无法登录）；业务路由一律 Bearer。
- **异步任务**：`POST /projects/:pid/chapters/:cid/generate` → 入 Redis 队列返回 202（不阻塞 HTTP）→ worker 消费 → `GET /tasks/:tid/events` SSE 推送进度（token/耗时/当前节点）。长生成不占连接。
- **错误语义**：409（冲突/状态不允许）、429（限流/每日建书配额）、403/404。worker 侧 4xx 不可重试、5xx/网络可重试。
- **契约防漂移**：35 条路径 OpenAPI 契约 + `aiink contract export && git diff --exit-code` 进 CI；Go 端契约测试 + Python 端回归——**Go/Python 双端不各说各话**。
- **前端细节**：SPA fallback 回退 index.html + 路径穿越防护（反斜杠归一化 + 段级 `..` 判断）。

---

## 十二、数据库选型（「为什么是 PostgreSQL 而不是 MySQL/MongoDB」）

**口径**（每个理由都能展开讲 30 秒）：
- **一库多用（核心理由）**：pgvector 扩展让 PG 同时当关系库 + 向量库——embeddings 表 + HNSW cosine 索引，**不引入独立向量库**（Milvus/ES）。少一个组件 = 少一套运维，事务和向量同一套连接、同一套备份。
- **RLS 原生多租户**：`FORCE ROW LEVEL SECURITY` 让数据隔离在**数据库层强制**，不是每处查询自觉带 where——MySQL 没有对等能力。
- **事务性刚需**：章节重写的「失效重建」必须原子（新写失败回滚则旧记忆不失效）——关系库事务是硬需求，这决定了记忆层不能上 NoSQL。
- **JSON 列半结构化**：候选 payload / 审计 evidence / 报告 findings 存 JSON；白名单靠 CheckConstraint（kind 枚举等）防坏数据落库炸批次。
- **索引按真实热键设计**：RLS 让所有查询恒带 project_id → 组合索引前缀必是 project_id；向量表配 HNSW；观测表 agent_runs 按 task_id 前缀扫。
- **Redis 只放可重建数据**：任务队列 / 租约锁 / 限流计数——丢了可重建，终态落 DB（Redis 不持久化不承诺）。
- **连接角色分离**：应用角色（NOBYPASSRLS）+ admin 角色（DDL/迁移/RLS）——权限最小化。
- **上量了怎么办（诚实+方案）**：MVP 不分库分表；向量量级上来先评估 pgvector 分区 / 独立向量库（`VectorStore` 抽象接口已预留切换点）。「我知道当前量级的边界在哪。」

---

## 十三、评测与量化（「甩数据收尾」的收尾题）

**Q：怎么评测你的系统？**

**口径**：
- **自建 40 条冲突样例评测集**（阳性 26 + 阴性 14），逐例验收：**检出率 80.8%（21/26）、误报率 0%（0/14）**。阴性样例是刻意构造的（刻意呼应、合法联手、支线休眠），保误报率
- **契约回归闸**：35 条路径 OpenAPI 契约，`aiink contract export && git diff --exit-code` 防漂移
- **466 项 Python 测试** + Go 契约测试 + 前端 45 单测 + 严格 TS 构建
- 诚实说 MVP 没做「在线 A/B + 人工抽检」——**说出没做什么，比假装全做了可信**

---

## 十四、踩坑故事（面试官最认这个，准备「标准回答 → 追问升级 → 踩坑分享」三层）

1. **DeepSeek thinking disabled 偶发失效**：content 全空白（甚至 122 个空格抓不住）→ `reasoning_content` 兜底 + 空白快速重试（不 sleep 区别于网络退避）——「我踩过模型行为不确定的坑，知道怎么处理」
2. **LangGraph resume 语义坑**：1.2.9 的 invoke-resume 是**整图从 START 重跑不是断点续跑**——确认流 resume 会重新命中 critical 永久暂停、正文永不落库（用户实测）→ 改 `finalize_awaiting_review` 直接取 checkpoint 草稿落库，不走图重跑。「我知道框架语义和直觉不一样」
3. **RLS 超户绕过坑**：超级用户永远绕过 RLS → 业务角色必须 NOBYPASSRLS + 双角色分离（admin 只做 DDL/迁移，业务不碰）
4. **checkpoint 三表无 FK 级联**：整书删除必须显式按 thread_id + LIKE 清理，否则残留现场
5. **网关 30s 转发超时 502**：长生成端点要 180s 长转发
6. **LLM 非严格 JSON 逼出来的解析器进化**：无差别正则删控制字符把多行 JSON 转坏 → 改状态机只转义字符串内（详见「结构化输出」章防线 2）

**「为什么不用公司现成 agent / Cursor」类边界问题**：数据路径可控、记忆层与业务绑定、评测指标与业务定义一致、排期回滚不绑外部发版节奏——主权在自己手里。

---

## 附：核心数字速查表

| 项 | 数字 |
|---|---|
| 冲突样例评测 | 检出率 80.8%（21/26）、误报率 0%（0/14） |
| Python 测试 | 466 通过 + 5 预期失败 |
| 契约回归 | 35 条路径 |
| 前端测试 | 45 单测 + 严格 TS + Vite 生产构建 |
| 召回 token 预算 | 12k（11–15k） |
| 修订预算 | max_revisions=2 / max_replans=1 / max_tool_calls=3 |
| 批次上限 | 默认 5 / 硬上限 20 |
| 调用层重试 | 指数退避 1/2/4s，最多 3 次 |
| 输出自修复 | `_llm_checked` 修正重试 `_LLM_CHECK_RETRIES=2`；JSON 解析三级容错（raw_decode→控制字符→裸引号） |
| worker 重试 | 3 次，2^retry 秒退避；书忙 defer 5s 不计重试 |
| 前缀缓存 | 价格差约 50 倍 |
| 长线阈值 | 伏笔 planted 15 章 / developing 10 章；主线停滞 15 章；桥段重复距 <0.3 且章距 ≥10 |
| 全局审计 | 每 10 章触发一次跨章抽样 L2 |
