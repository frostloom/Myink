# 面试问题·简历对齐版（逐条从简历反推）

> 面试官 80% 的问题是从你的简历里长出来的：你写什么、他就问什么。这份清单按简历原文逐条反推，**每条简历短语都对应一组必问题**，并把「面试官为什么这么问」标注出来。
>
> 第 1 节是**方法论**（可复用到任意项目）；第 2 节是**本项目的完整反推结果**（每题带简历出处 + 意图 + 答题要点）。

---

## 一、简历 → 问题的反推方法论（发给其他项目也用它）

面试官的行为模式：**从简历里挑「最重的话」往死里问**。关键词密度越高的短语 = 被追问概率越高。按这个表反推：

| 简历条目（示例句式） | 面试官会顺着问的 | 为什么这么问（意图） |
|---|---|---|
| 解决 XX 痛点 / XX 业务问题 | 挑最痛的 1-2 个往死里问：怎么检测？怎么防？**效果数字**？ | 验证你真做过，不是背话术 |
| 基于 XX 框架实现 | 为什么选它？它有什么缺陷？**哪部分你改写了框架默认语义**？ | 考深度，区分调包侠 |
| 多 Agent 编排 / agentic workflow | 拆几个？为什么拆？**控制权在代码还是 LLM**？和 Workflow 区别？ | 必考概念题 |
| XX 范式（Reflexion / ReAct / CoT） | 范式是什么？**落地成什么数据结构**？闭环怎么量化？ | 考「范式→工程」的翻译能力 |
| 审核 / 路由 / 校验 | 路由条件是什么？为什么 LLM 判而不是规则判？**误判怎么办**？ | 考一致性治理 |
| 三层记忆 / 召回 / RAG | 三层怎么分？召回具体怎么跑？**上下文预算**？遗忘？ | 记忆+RAG 是高频区 |
| XX存储 + YY检索融合（向量+关键词） | 为什么融合？各自解决什么？**融合算法（RRF）细节**？ | 考是不是真懂混合检索 |
| 自创 / 独创 XX | **相比现成方案好在哪？边界在哪？** | 「自创」= 必被深挖 |
| 网关 / 解耦 / 独立扩缩容 | 为什么拆分？通信协议？高并发怎么扛？部署拓扑？ | 考后端工程 |
| 多租户 / 隔离 / 权限 | 隔离机制？**绕过风险**？漏洞边界？ | 考安全思维 |
| MCP / 协议 / 外部接入 | 为什么用这协议？接了什么？**数据安全边界**？降级？ | 考协议理解 + 安全意识 |
| 追踪 / 可观测 / 状态机 | 流转数据流？状态机怎么设计？**失败恢复**？ | 考系统设计 |
| 三态控制 / 转人工 | 状态图？**人工介入怎么设计**？续跑语义？ | 考 HITL + 状态管理 |
| 成本 / 可视化 / 审计 | 怎么记录？为什么透明？**省了多少 / 量化**？ | 考工程意识，加分项 |
| 一键启动 / 部署 | 部署架构？镜像？横向扩展？ | 检验完整闭环能力 |

---

## 二、本项目：简历逐条反推的问题清单

### 📄 项目介绍（简历第 1 段）

**Q1｜介绍一下这个项目。**（简历出处：项目介绍）
- **意图**：30 秒看你能不能把「五大痛点 → 解决方案」串成因果链，而不是倒技术栈。
- **要点**：STAR + 因果链。人设混乱→硬约束台账；战力崩坏→L1 境界规则；前后矛盾→L1+L2 校验；长上下文过载→三层记忆；AI 味→文风档案+复盘。闭环 = 单章/批次生成 → 一致性校验 → 记忆沉淀。结尾数字：检出率 80.8%、误报率 0%、466 测试。

**Q2｜AI 味怎么解决？（简历明写了 AI 味是五大痛点之一，必问）**（简历出处：五大创作痛点）
- **意图**：五大痛点前四个 cheatsheet 都覆盖了，唯独 AI 味是「你的差异化词」，面试官专挑简历里他没听过/你没准备的词问。
- **要点**：三个机制叠加——
  1. **文风档案**（`style_extract.py`）：从读者样本提取「统计层」（句长三档分布/对话密度/高频 2-gram）+ LLM 提炼（pov/句式/词汇修辞/对话腔调/**禁忌清单**），落 `StyleProfile` 注入写作 prompt——模仿样本文风而不是 AI 模板腔；
  2. **桥段重复检测**：当前章事件 vs 历史事件余弦近邻，距离 <0.3 且章距 ≥10 判偷懒重复——拦「套路化桥段」；
  3. **复盘经验注入**：reflexion 高危经验确认后注入后续写作，越写越懂这本书。
- **数字**：桥段距离 <0.3、句长三档 15/40 字阈值。

**Q3｜单章生成和批次生成是什么？为什么要有批次？**（简历出处：单章/批次生成）
- **要点**：批次 = 先规划 N 章（默认 5、硬上限 20）→ 逐章走 chapter 子图 → 批次收尾复盘。好处：规划有全局视野（伏笔收放跨章）、批次失败断点续跑、批次级共享池省 token。

**Q4｜Docker 一键启动怎么做的？**（简历出处：Docker一键启动）
- **要点**：docker-compose 编排（网关/API/worker/Postgres+pgvector/Redis），api 容器不暴露端口只走网关（内存 E2E 坑），单命令起全栈，适合完整演示。

### 📄 第 1 段：多Agent编排+审核路由

**Q5｜为什么拆成这么多 Agent？怎么拆的？**（简历出处：多Agent编排）
- **要点**：Planner/Writer/Extract/Audit/Revise。单 Prompt 注意力分散 + 可单测。写前/写后/抽取/校验职责分离。

**Q6｜agentic workflow 怎么体现？控制权在哪？**（简历出处：agentic workflow）
- **要点**：诚实答「确定性 Workflow + 局部 Agent 决策点」。流程控制权在代码（两层图），只有 audit 语义层 LLM 自主决策三级路由。为什么刻意：可复现、可审计、不烧钱。write/audit 持只读查证工具 `READ_TOOLS`，模型自主决定查不查——这是 agentic 的点。

**Q7｜审核中枢 Agent 的三级路由 pass/rewrite/replan 怎么设计的？**（简历出处：审核中枢三级路由）
- **要点**：audit 节点输出 verdict 三分——pass（放行）/ rewrite（L1 critical 或 L2 major，revise 重写）/ replan（规划漂移，重规划）。判据带 `evidence` 逐字引用 + `confidence`，不是凭感觉。数字：max_revisions=2、max_replans=1（重规划比重写贵）。
- **深挖**：为什么路由层用 LLM 而不是纯规则？→ 语义层（战力通胀、伏笔烂尾、主线停滞）规则写不出，规则只管确定性硬约束（境界跳级/死而复生/敌我协作）；L1 先跑省成本，LLM 兜语义。

**Q8｜Reflexion 范式批次复盘怎么落地的？犯错-复盘-少犯闭环怎么转起来？**（简历出处：Reflexion批次复盘）
- **要点**：批次收尾 reflexion 把**高危经验**（源自 critical/major finding）写入 `writing_lessons` 待确认池（`status=proposed`）→ 人工确认 → `active` → 下一批 recall 注入后续章节规划/写作——「越写越懂这本书」。闭环：犯错 → 审计抓出来 → 沉淀经验 → 确认 → 注入 → 少犯。
- **深挖**：为什么复盘经验要人工确认才生效？→ 数据流边界（§6.2 编排层写库），LLM 自产自销经验可能带幻觉，确认=防幻觉记忆进事实层；且可审计。
- **数字**：proposed→active 两态；经验随批注入。

### 📄 第 2 段：三层记忆+混合召回

**Q9｜短期/中期/长期三层记忆分别是什么？为什么这么分？**（简历出处：三层记忆）
- **要点**：短=上章摘要（80-150字）；中=事件（结构化、天然检索单元）；长=硬约束事实（恒在 Top-K 永不丢）。按「遗忘成本」分：设定不能忘、事件可过期、摘要最短命。

**Q10｜十万字长文上下文过载怎么解决？远期设定丢失怎么解决？**（简历出处：十万字长文/远期设定丢失）
- **要点**：过载→12k token 预算分层截断（中文 1 字≈1.4 token，不引 tokenizer）+ `_CONTENT_CAP=300` 截单条 + 批次级共享池（hard_facts 组装一次各章复用）；丢失→硬约束恒在 Top-K 不参与相似度截断（主角死没死、战力上限是权威数据，LRU 驱逐会杀设定）。

**Q11｜为什么 pgvector？向量检索怎么做的？**（简历出处：PostgreSQL+pgvector）
- **要点**：一库多用（关系+向量同库同事务同备份，不引 Milvus/ES）+ RLS 多租户原生支持。bge-m3 编码，HNSW 过滤索引（先 project_id 过滤后相似度排序）。

**Q12｜为什么关键词检索 + 向量检索 RRF 融合？**（简历出处：向量+关键词RRF融合召回）
- **要点**：纯向量三个致命问题——精确匹配不行、术语召回差、专名（人名/地名）漏。向量腿（上章摘要语义近邻 top-5）+ 关键词腿（人物名对事件摘要 ILIKE top-5）→ RRF k=60 跨腿加分。差异点：关键词腿用 ILIKE 不用 BM25——中文无分词，人名精确子串匹配更可靠；trade-off：ILIKE 走不了 btree，用 project_id 过滤 + LIMIT 封顶。

**Q13｜自创关系图谱怎么存人物关系？相比普通表好在哪？（简历写了「自创」，必深挖）**（简历出处：自创关系图谱）
- **要点**：`relations` 表——source_id/target_id/relation_type（白名单 CheckConstraint）+ **时间窗** valid_from/valid_to（追加式不覆盖，当前关系 = valid_to IS NULL 最新行，历史可追溯）+ 双向索引（project+source / project+target）支持 1-2 跳查询（`get_relations` 按 source IN / target IN 双向查）。
- **自创点**（为什么叫自创）：通用图数据库（Neo4j）是重组件且难和 RLS/事务同层；我这套 = **关系表 + 时间窗 + 双向索引**在 PG 里模拟图谱查询，1-2 跳够创作用，且和记忆同事务、同 RLS 隔离。trade-off：超过 2 跳的复杂图查询性能不行，MVP 场景用不到。
- **数字**：1-2 跳、两枚索引、白名单 RELATION_TYPES。

### 📄 第 3 段：Go网关+多租户+MCP

**Q14｜为什么 Go 网关 + Python 推理层拆分？独立扩缩容怎么体现？**（简历出处：Go网关+Python推理层HTTP解耦）
- **要点**：Go 扛高并发面（JWT/令牌桶/SSE/转发），Python 专注推理编排。HTTP 解耦 → 网关可多副本、推理层可单独扩（worker 横向扩展），互不阻塞。选型理由：Go 高并发 + 低内存，HTTP 解耦不引 RPC/消息耦合。

**Q15｜Go 网关怎么扛高并发？限流怎么做？**（简历出处：限流/鉴权/SSE）
- **要点**：Gin :8080 唯一入口；JWT 鉴权 + **令牌桶**限流 + 三层闸门 + SSE 推送 + 转发 Python :8100；前端静态产物同源托管单端口出页面+API。

**Q16｜SSE 推送实时流转为什么用 SSE 不用 WebSocket？**（简历出处：SSE实时流转）
- **要点**：单向服务器→客户端推送，SSE 够用且简单（HTTP 原生、自动重连、过代理友好）；WebSocket 双向成本高，本项目无客户端→服务器长连接需求。

**Q17｜多租户隔离：PG RLS + SET LOCAL 怎么做到默认拒绝越权？**（简历出处：PG RLS+事务级SET LOCAL默认拒绝越权）
- **要点**：RLS FORCE + 事务级 `SET LOCAL app.tenant_id`（事务结束自动恢复，防连接池污染）+ 业务角色 NOBYPASSRLS + admin/业务双角色分离（超户永远绕过 RLS，只做 DDL/迁移）。**坑**：`SET LOCAL` 提交后会话 tenant 是 `''` 不是 NULL，策略用 `NULLIF(current_setting,'')` 判空，否则裸 `::uuid` DataError。

**Q18｜MCP 是什么？为什么用 MCP 接榜单？接了什么工具？安全边界？**（简历出处：MCP标准协议外接热门榜单）
- **要点**：MCP 是模型上下文协议（工具接入标准化），`integrations/mcp.py` McpClient 实现 connect/list_tools/call_tool 四件套；外接热门榜单 → 提供写作灵感（蹭热点/借鉴桥段）。**关键安全设计**：MCP 工具**不暴露给 LLM agent 自由调用**——编排层受控原子能力，调用方拿到原始输出必须 sanitize；网络不可达返回内置样例降级（不 500 不阻塞）。
- **深挖**：为什么不用直接 HTTP 调榜单 API？→ MCP 是标准协议，换数据源不用改业务代码，可插拔。

### 📄 第 4 段：节点流转追踪+任务控制+成本可视

**Q19｜worker 逐节点写 Redis Stream + SSE 推送，流转追踪怎么做？**（简历出处：worker逐节点写Redis Stream）
- **要点**：worker 每个节点执行后 `record_run` 写 Redis Stream（token/耗时/当前节点/结果）→ 网关 SSE 推前端；`GET /tasks/:tid/events` 订阅。可观测 = 每步可回放。

**Q20｜批次 pause/resume/cancel 三态怎么设计？**（简历出处：批次三态控制）
- **要点**：批次任务状态机 paused/running/canceled + worker 消费前查状态（已处理/取消/他 worker 在跑 → skip 直接 ACK）。resume = checkpoint 断点续跑（thread_id=task_id，失败章从断点继续、已完成章不重跑）。

**Q21｜critical 冲突自动暂停转人工，人工流程怎么设计的？**（简历出处：critical冲突自动暂停转人工）
- **要点**：L1 critical / L2 major 超预算（max_revisions=2/max_replans=1）→ 候选进池 + 章节 `awaiting_review` + 批次暂停 → 人工确认后 checkpoint 续跑。HITL 用业务层状态机承载（可审计、可查询、可断线恢复），不用框架原生 interrupt。

**Q22｜每节点 token/成本/耗时记录 + 前端每章/每任务花费展示，为什么做成本透明？**（简历出处：成本透明可审计）
- **要点**：agent_runs 表逐节点记录（输入/输出 token、cost、耗时、retry_count）→ 前端每章/每任务总花费实时展示。**为什么**：LLM 项目最大的运营风险是成本失控，透明 = 可审计 + 逼自己省成本。省的手段：50 倍前缀缓存、JSON mode+thinking disabled、L1 把一半校验挪到规则、预算闸门、批次共享池。

### 📄 第三段：简历外的通用必问补强（无论简历写没写都会问）

**Q23｜LLM 返回不是严格 JSON 怎么解决？** → 四道防线（请求侧强制/解析侧容错/校验侧拒绝/失败侧自愈），详见手册九章。
**Q24｜组件挂了/超时/返回坏了怎么办？** → 调用层 1/2/4s×3 + 空白快速重试；任务层 3 次 2^retry、429/5xx 可重试 4xx 不可、书忙 defer 不计；图层断点续跑；模型降级链。
**Q25｜幂等怎么保证？** → worker 消费前查状态、persist 判重、新人物去重、成本按 task_id 前缀聚合。
**Q26｜怎么评测你的系统？** → 40 条冲突样例（检出率 80.8%/误报率 0%）、35 条路径契约回归、466 测试、诚实说没做在线 A/B。
**Q27｜为什么 PostgreSQL 不是 MySQL/MongoDB？** → 一库多用（pgvector）、RLS 原生多租户、事务性刚需、JSON 半结构化、Redis 只放可重建数据。
**Q28｜讲一个踩得最深的坑。** → 六个任选（thinking disabled 失效 / LangGraph resume 整图重跑 / RLS 超户绕过 / checkpoint 无级联 / 网关 30s 502 / 非严格 JSON 逼出解析器进化）。
