# 面试问答生成器（发给任意 AI / Agent 项目）

> 用法：复制下面【📤 转发给项目的提示词】整段，粘贴给你的项目（GitHub 仓库 / 代码目录），让它按 32 道题目输出对应问答。项目作者/项目简介留空或替换。

---

## 📤 转发给项目的提示词

你是「{项目名}」的资深作者和架构师，接下来我要模拟面试官对你提问。你只基于这个项目的真实代码、真实实现、真实数字作答，禁止编造。

**第一步：先反推问题（面试官是照简历问的，不是照通用清单问的）。**
把这份项目的简历描述（如果没有，就把下面「项目一句话简介」换成完整简历）逐条拆开，找出里面每个「关键词密度高的短语」——多Agent编排、XX范式、自创XX、MCP、多租户、RLS、SSE、三态、成本可视等——每个短语反推出 1~2 道面试官一定会追问的问题，连同下面的通用题一起作答。

**答题铁律（每条答案都必须遵守）：**
1. 每个答案结尾带一个数字（真实量级，如调用次数、token 预算、测试数、价差、阈值）或一个 trade-off（「我用了 X 而不是 Y，因为 Z」）。
2. 被问住/没做的部分，诚实说「这是 MVP 没做的，如果上生产我会……」，并给出后续方案——不要硬编。
3. 别一上来倒技术栈，先讲业务因果链（解决什么问题 → 输入输出边界 → 谁把自然语言变成可执行步骤 → 谁调工具 → 失败后状态落在哪一层），技术是承载因果链的手段。
4. 每条答案控制在 3~6 句话，能展开的标注「追问可深挖」。
5. 涉及代码给出 `文件路径:行号` 引用，方便我核实。

项目一句话简介：{你的一句话简介}

下面 32 道题，逐题输出「**Q：** 问题 → **A：** 答案」格式。

## 一、开场类
1. 介绍一下你做的这个项目（用 STAR + 因果链：业务解决什么问题、输入输出边界、谁把自然语言变成可执行步骤、谁调工具、失败后状态落在哪一层）。

## 二、Agent 概念
2. Agent 的核心组件是什么？和 Workflow / 普通链式调用的本质区别是什么？
3. ChatBot + 插件算 Agent 吗？RAG + Chat 算吗？

## 三、架构设计
4. 为什么拆成这么多 Agent？拆多 Agent 的理由是什么？
5. 为什么选 Hierarchical（两层图）而不是 Router / Pipeline？
6. 你的编排框架比 X（如 LangChain）好在哪？它有什么缺点？
7. 你的编排和简单 if-else 路由有什么区别？

## 四、编排机制
8. State 怎么设计？TypedDict 还是 Pydantic？为什么？
9. 断点续跑怎么做？Checkpointer / 状态持久化怎么选型？
10. Human-in-the-Loop（人工介入）怎么做？
11. 并行怎么处理？为什么这样取舍？

## 五、记忆 / RAG
12. 短期 / 长期记忆怎么设计？
13. 上下文越来越大怎么办（上下文管理 / 预算控制）？
14. 有遗忘机制吗？
15. 召回策略具体怎么跑？
16. 为什么用混合检索？BM25 + 向量为什么好？
17. Chunk 切割策略是什么？为什么这么切？
18. Rerank 做了吗？为什么做 / 不做？

## 六、幻觉 / 一致性 / 结构化输出
19. 怎么减少幻觉 / 保证输出一致性？
20. 多个 Agent 任务冲突怎么处理？
21. 怎么控制校验 / 审查 Agent 的误判率？
22. 检索 - 生成对齐怎么做的（防止模型生成与检索内容脱节）？
23. LLM 返回的不是严格结构化数据（JSON）怎么解决？

## 七、工程化 / 可靠性
24. 某个组件 / Agent 挂了、超时、返回坏了怎么办（重试 / 降级 / 断点）？
25. 幂等怎么保证？
26. 成本控制怎么做（LLM 调用成本）？
27. 多租户 / 数据隔离怎么做？

## 八、接口 / 数据库
28. 接口怎么设计的？为什么这样（异步 / SSE / 错误语义 / 契约）？
29. 为什么用这个数据库而不是 XX（数据库选型）？

## 九、评测
30. 怎么评测你的系统？
31. 项目里最大的难点是什么？怎么解决的？（每个难点一句话本质 + 方案 + 数字）

## 十、踩坑
32. 讲一个你踩过的最深的坑 / 框架语义和直觉不一样的地方。
